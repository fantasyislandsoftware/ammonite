export enum EnumByteOrder {
  LOW = 0,
  HIGH = 1,
}

export enum EnumOpBit {
  BYTE = 8,
  WORD = 16,
  LONG = 32,
}

export type INumberCalcConfig = {
  log?: boolean;
};

export enum EnumBit {
  BYTE = 8,
  WORD = 16,
  LONG = 32,
}
