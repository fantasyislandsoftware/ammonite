import { IScreen } from 'interface/screen';
import { IWindow } from 'interface/window';
import { drawLine, fillRect } from './graphics';

export const renderWindow = (screen: IScreen, window: IWindow) => {
  /*fillRect(
    screen,
    window.position.x,
    window.position.y,
    window.width,
    window.height,
    1
  );*/
  //drawLine(screen, 0, 0, 200, 200, 1);
};
