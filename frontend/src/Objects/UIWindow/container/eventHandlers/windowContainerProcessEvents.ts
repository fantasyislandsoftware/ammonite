import { IEvent } from 'interface/event';

export const windowContainerProcessEvents = (event: IEvent) => {
  //console.log(event.objects.window?.titleBar?.title);
  //console.log(event.mouse?.position.x, event.mouse?.position.y);
};
