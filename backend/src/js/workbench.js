define(wbScreenId, 0);
define(testWindowId, 0);
define(system, 0);
define(canvas, 0);
openScreen(320, 256, low, "Workbench", wbScreenId);

//drawImage(screen, wbScreenId, 0, 0, 0, 0);

label(START);
jmp(START);
